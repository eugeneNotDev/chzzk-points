// Supabase 클라이언트 초기화.
// SUPABASE_URL / SUPABASE_ANON_KEY는 공개돼도 되는 값 (Row Level Security로 접근을 제어).
// 절대 여기에 service_role 키나 치지직 clientSecret을 넣지 말 것 — 그건 supabase/functions에서만 쓴다.

// TODO: import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
// TODO: export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)

const SUPABASE_URL = "";
const SUPABASE_ANON_KEY = "";
